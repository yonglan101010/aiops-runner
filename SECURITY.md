# 安全策略

## 支持范围

| 版本 | 状态 |
| --- | --- |
| `0.1.x` | 技术预览，接受安全修复 |
| 更早版本 | 不支持 |

技术预览不代表生产就绪。部署者应自行完成权限、网络、备份、密钥轮换和端到端验收。

## 私密报告漏洞

请使用本 GitHub 仓库的 **Private Vulnerability Reporting** 提交漏洞，不要创建公开 Issue。
报告应包含受影响版本、复现步骤、影响范围和建议修复方向，但不得包含真实 Token、私钥、
kubeconfig、生产地址、原始告警或回调正文。

如果仓库暂时未显示私密报告入口，请不要公开漏洞细节；等待维护者启用该功能后再提交。

维护者会尽力确认报告、评估影响、准备修复并在适当时间协调披露。技术预览阶段不承诺固定响应时限。

## 凭据意外泄露

凭据一旦进入提交、日志、Issue 或截图，应立即视为泄露并在对应系统轮换；删除当前文件不足以消除
Git 历史风险。报告中只提供凭据类型、影响组件和已完成的轮换状态，不要再次粘贴原值。
